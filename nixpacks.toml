# Nixpacks configuration for Django on Railway

[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py collectstatic --no-input"]

[start]
cmd = "python manage.py migrate && python manage.py ensure_admin && python manage.py ensure_site && gunicorn config.wsgi:application"
