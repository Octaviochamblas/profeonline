# Reporte de sesión — 2026-06-17

**Agente:** 🏛️ Claude · **Rama:** `feat/guias-desde-drive` → **PR #68** (abierto, pendiente auditoría)

## Avances desde el último reporte

- **Fix `generate_pending_questions`:** el pie `[ok]` ya no se imprime cuando una celda falla o
  genera 0 (era engañoso tras un 503). Se diagnosticó el 503 de Gemini como saturación transitoria
  (el backoff ya estaba); nada que arreglar ahí.
- **Feature "Guías desde Google Drive" construida (PR #68, 4 commits):**
  - `apps/content/services/drive_service.py` (nuevo): service account + REST de Drive v3 con
    `google-auth` (import perezoso). `is_configured`, `parse_folder_id`, `list_folder` (subcarpetas +
    archivos), `fetch_file` (export de Google Docs / download + extractor para PDF/txt).
  - `views/quiz_guides.py`: **navegación de subcarpetas** (`?drive_folder=<id>`), `import_drive_guides`,
    y `_link_tree` (selector jerárquico). `templates/partials/guide_link_picker.html` (nuevo).
  - `templates/pages/quiz_guides.html`: sección "📂 Desde Google Drive" + **selector jerárquico**
    Asignatura ▸ Tema ▸ Recurso (`details` nativo, sin JS, CSP-safe). Link "Guías de referencia" en navbar.
  - `settings`: `GOOGLE_SERVICE_ACCOUNT_JSON` + `GUIDES_DRIVE_FOLDER_ID`. `requirements`: `google-auth`
    + `python-docx` (Word ahora extrae; arregla también la subida manual que fallaba en silencio).
  - `tests/test_drive_guides.py` (13). **Suite completa verde · `check` OK · sin migraciones.**
- **Validación end-to-end** contra la carpeta real "Material Académico": navegación OK; PDFs propios y
  `.docx` extraen bien; el transcript del video baja OK (desde IP residencial); **libros escaneados no
  extraen** (son imágenes; sin OCR — se descartan en el import).
- **Generación validada en vivo:** 5 preguntas reales (Nivel 2/Evaluación) **correctas 5/5**;
  comparación con/sin transcript; comparación en tema difícil (integración por partes): la IA acierta
  el cálculo estándar **con o sin** anclaje — el transcript aporta **fidelidad a la clase** (usa el
  ejemplo del profe), no tanto corrección. **Consumo medido:** ~2.000–3.700 tokens por llamada
  (el transcript agrega ~1.500 de entrada y se re-manda en cada celda).
- **Default → publicar de inmediato** (comando `generate_pending_questions` + checkbox del estudio),
  por preferencia del 🧑. 5 preguntas publicadas en "1.3 Relaciones de Orden" (local) + transcript guardado.

## Decisiones importantes

- **Drive = service account + carpeta compartida** (no OAuth por usuario, no Google Picker): todo
  server-side, no toca la CSP. Carpeta configurable desde el panel; secreto único en una variable.
- **Sin OCR ni recursión automática en v1**; la navegación de subcarpetas cubre la biblioteca anidada.
- **Publicar preguntas de inmediato por defecto** (revierte el "generar→revisar→publicar" del handoff
  del banco). Motivo del 🧑: "en borradores no sé cómo se ven". Guardado en memoria. *Tradeoff:* saltea
  revisión → en niveles difíciles conviene chequear; se puede editar/despublicar desde la revisión.

## Estado del proyecto / notas

- **PR #68** abierto; pendiente auditoría 🧩 Codex → merge (auto-merge con label `audit:aprobado`).
- **Hallazgo importante:** los loops en prod corrían con el default viejo (borrador), así que las
  preguntas ya generadas en prod **quedaron ocultas**; hay que publicarlas (pasada única).
- Quedaron 2 dev servers locales levantados (8000 con código previo, 8001 con el actual) — apagar.
- `backfill_trickle.ps1` / `generate_loop.ps1` siguen sin trackear (scripts operativos del 🧑).

## Pendientes / Próximos pasos

1. Auditoría 🧩 Codex + **merge de PR #68**.
2. 🧑: cargar `GOOGLE_SERVICE_ACCOUNT_JSON` + `GUIDES_DRIVE_FOLDER_ID` en Railway antes de usar en prod.
3. **Publicar los borradores que ya están en prod** (comando puntual, requiere `PROFEONLINE_DB_URL`).
4. Plan de **transcribir videos + generar anclado**, recurso por recurso (a cuentagotas por la cuota).
5. **Producto (no construido, solo conversado):** 3 pilares — evaluación correcta (flujo de calidad),
   más ejercicios (práctica hasta dominar), **planes adaptativos al ritmo** — + **OCR** (Gemini Vision)
   para escaneados.
6. Mejoras opcionales: loguear `usageMetadata` (tokens reales por corrida), estimador de campaña,
   dedup al reimportar guías, extraer tablas de `.docx`.
7. Arrastrado: **rotar la contraseña de Postgres** (quedó expuesta en una captura).
