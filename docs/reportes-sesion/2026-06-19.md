# Reporte de sesión — 2026-06-19

## Avances desde el último reporte
- **Ejecución del script en Producción:** Se ejecutó con éxito el script `scratch/generate_math_questions.py` contra la base de datos de producción de Railway.
- **Inserción aditiva de preguntas:** Se agregaron exitosamente **776 nuevas preguntas matemáticas** distribuidas entre los 10 recursos correspondientes al tema "Números enteros".
- **Sin efectos destructivos:** La ejecución fue estrictamente aditiva, respetando todas las preguntas preexistentes en los recursos y evitando enunciados duplicados.

## Decisiones importantes
- **Conexión mediante proxy público de Railway:** Para evitar el problema de resolución del host interno (`postgres.railway.internal`) desde fuera de la red privada de Railway, se utilizó el túnel público configurado en el puerto `22674` (`zephyr.proxy.rlwy.net:22674`).
- **Pacing y replicación:** Las preguntas fueron asignadas sistemáticamente para cubrir exactamente los pools de preparación (práctica), evaluación y comunes ("ambas") en los niveles 1, 2 y 3.

## Estado del proyecto / notas
- Conteos finales de preguntas por recurso en la base de datos de producción:
  1. `12-conjuntos-numericos`: 136 preguntas (antes 53)
  2. `13-numeros-enteros-relaciones-de-orden-mayor-menor-e-igual`: 140 preguntas (antes 52)
  3. `14-valor-absoluto-relaciones-de-orden`: 136 preguntas (antes 52)
  4. `15-regla-de-signos-para-sumasrestas`: 116 preguntas (antes 28)
  5. `15a-ejercicios-de-sumas-y-restas-aplicacion-de-regla-de-los-signos`: 116 preguntas (antes 28)
  6. `16-regla-de-los-signos-en-multiplicaciondivision-ejemplos`: 104 preguntas (antes 24)
  7. `17-prioridad-de-operaciones-sumarestamultiplicaciondivision-combinadas`: 115 preguntas (antes 27)
  8. `18-numeros-primos-multiplos-y-divisores`: 74 preguntas (antes 23)
  9. `19-minimo-comun-multiplo-maximo-comun-divisor`: 91 preguntas (antes 28)
  10. `19a-ejercicios-minimo-comun-multiplo`: 91 preguntas (antes 28)

## Pendientes / Próximos pasos
- **Verificación visual de la analítica en producción:** Acceder al panel de administración y confirmar que la cobertura para el tema "Números enteros" se encuentre al 100% y sin déficits.

## Auditoría de diversidad del banco de racionales — Codex
- Se detectó que las 1.428 preguntas originales correspondían a aproximadamente 55 moldes
  pedagógicos; la mayoría solo cambiaba números, nombres o la etiqueta "caso/variación".
- Se prototipó una capa de diversificación en `scratch/generate_racionales_questions.py` con seis
  familias cognitivas equilibradas por nivel.
- La auditoría local quedó verde para **1.440 preguntas en 16 recursos**: 90 por recurso, sin
  enunciados ni alternativas duplicadas y con una única respuesta correcta.
- Se eliminó el borrado implícito: el script audita por defecto; escribir exige `--apply` y
  reemplazar preguntas exige `--apply --replace`.
- En esta primera fase no se ejecutó contra producción; la aplicación autorizada se documenta a
  continuación.

## Aplicación en producción — diversidad de racionales
- Tras autorización explícita del usuario, se ejecutó la curación sobre los 16 recursos de números
  racionales.
- Antes de escribir se exportaron las **1.406 preguntas originales** con sus alternativas a
  `backups/racionales_questions_before_20260619T132910Z.json`.
- Se conservaron publicadas **55 representantes**, una por molde pedagógico antiguo, y se archivaron
  **1.351 repeticiones**. Se usó `archivada` en vez de borrado físico para preservar rollback e
  historial.
- Se publicaron **1.440 preguntas nuevas**: 90 por recurso, 30 por nivel y 10 por cada modo
  (`preparacion`, `evaluacion`, `ambas`).
- Verificación final en producción: **1.495 publicadas**, **1.351 archivadas**, 0 fallos de
  distribución y 0 preguntas con un número inválido de alternativas o respuestas correctas.
- Smoke HTTP posterior: `https://www.profeonline.cl/` respondió **200**.

## Limpieza física y auditoría global del banco
- Se guardó la especificación del futuro generador universal por tema en
  `backlog/1-por-iniciar/generador-universal-preguntas-por-tema.md`.
- Se creó un segundo respaldo de las **1.351 preguntas archivadas** y sus **5.404 alternativas**:
  `backups/archived_questions_before_delete_20260619T162832Z.json`.
- Se reconfirmó que ninguna pregunta archivada tenía respuestas de alumnos ni reportes y se
  eliminaron físicamente dentro de una transacción.
- Estado final de producción: **2.476 activas**, **0 archivadas**, **0 alternativas huérfanas**.
- La auditoría global encontró **0 duplicados textuales** y **43 grupos estructurales** con
  **812 preguntas**; se conservaron todas para revisión pedagógica manual.
- Informes: `auditorias/2026-06-19-auditoria-global-preguntas.md` y `.json`.
- Smoke final de `https://www.profeonline.cl/`: **HTTP 200**.

## Pipeline único de publicación educativa basado en transcripción — Codex
- Se auditó el flujo vigente y se confirmó que el “script local de Codex” citado por la
  documentación no existía como artefacto persistido; la Fase 2 de uploader/agente seguía pendiente.
- Se creó la tarjeta `pipeline-publicacion-transcripcion.md` y se avanzó por arquitectura,
  construcción y auditoría técnica hasta `backlog/5-cierre/`.
- Se implementó `PublicationItem`, con estado durable e idempotencia por lote/archivo, y relaciones
  opcionales/aditivas para guía canónica y preguntas generadas por el pipeline.
- El webhook ahora mantiene recurso e ítem en borrador, guarda transcript y permite reintentos sin
  duplicar por ID de YouTube ni por `batch_id + source_filename`.
- Se añadió un orquestador que detiene transcripts insuficientes, genera documento pedagógico y
  metadatos coherentes, crea/actualiza guía y configuración, genera preguntas en borrador, ejecuta
  una segunda auditoría y regenera cupos rechazados.
- Se versionó `scripts/process_upload_batch.py`: sube no listado, maneja playlist, persiste un
  sidecar local para reanudar sin duplicar, obtiene transcript, espera `questions_ready`, actualiza
  YouTube y confirma el cierre.
- Se blindó el estudio para archivar —no borrar— preguntas con respuestas/reportes históricos y
  rechazar el borrado de alternativas respondidas.
- Validación: **341 tests OK** (594,9 s; el wrapper agotó timeout después de imprimir `OK`),
  15 tests focalizados finales OK tras el ajuste transaccional, `makemigrations --check --dry-run` sin cambios y
  `check --deploy` de producción sin errores (3 warnings operacionales conocidos con env dummy).
- No se ejecutaron migraciones, uploads, IA ni cambios contra producción.

## Auditoría de cierre del pipeline + correcciones (🏛️ Claude)
- Auditoría rigurosa read-only del backend del pipeline (modelos, migración, webhook, servicio,
  comando, endpoints) y del agente. Veredicto: aprobable con correcciones.
- Correcciones aplicadas: revert del agente a `unlisted` si la confirmación falla; `canonical_resource`
  `CASCADE → SET_NULL` (modelo + migración 0029); tests de regeneración/agotamiento/auth negativa.
- Validación local: **suite completa 348 tests OK**, focalizada 17 OK, `check` y
  `makemigrations --check` verdes. Sin migraciones aplicadas, sin red ni producción.
- **Decisión:** el agente Python `scripts/process_upload_batch.py` NO se mergea — duplica el uploader
  Node `profeonline-uploader`. Este push trae solo el **backend** (motor) vía PR a `main`. El flujo de
  subida de dos fases se integrará después en el uploader Node. Concurrencia (`select_for_update`) diferida.
