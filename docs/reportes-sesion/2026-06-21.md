# Reporte de sesión — 2026-06-21

## Avances desde el último reporte

- Se reorganizó el banco de cobertura como
  `Área → Nivel → Asignatura → Tema → Recurso`.
- Se fijó el orden pedagógico Escolar → Medio/Preuniversitario → Universitario.
- Se corrigieron las asociaciones de áreas y niveles solicitadas.
- Se pobló el tema Lenguaje Algebraico: 17 recursos, 1.530 preguntas y cobertura completa.
- Se configuró Lenguaje Algebraico con orden manual para respetar la secuencia
  `1.x → 2.x → 3.x → 4.01 → 4.01a → 4.02 → 4.03 → 4.04`.
- Tras revisión humana, se reemplazó el banco inicial de Lenguaje Algebraico
  por una versión v2 más concreta y contextualizada.
- 🏛️ Claude + 🧑: se versionó el **agente local de subida `upload-batch/v1`**
  (`scripts/process_upload_batch.py`), el comando `cleanup_borradores` y los tests
  del agente (commit `79836ad`). Cierra el paso 2 del pipeline único de publicación:
  el cliente Node se reemplaza por este agente Python local (sube no listado →
  transcript local → registra ítem → espera validación → publica; revierte a no
  listado si la confirmación server-side falla).
- 🧩 Codex: se construyó el **rediseño compacto de la página de tema** en
  `codex/topic-detail-compact-progress`: cabecera mínima, tarjetas sin metadatos
  repetidos y resumen académico por recursos/niveles disponibles.
- El progreso del tema ahora incluye los recursos no iniciados como cero; el caso
  de un recurso al 30% entre 19 recursos devuelve 2% global.
- Se agregó la limpieza editorial v1 de títulos, comando con dry-run, migración
  `0032_clean_resource_titles` y compatibilidad del seed con slugs históricos.

## Decisiones importantes

- Los recursos multinivel aparecen en cada rama de nivel, pero los agregados del
  área cuentan recursos únicos.
- El orden visible de niveles es fijo y pedagógico:
  Escolar → Medio/Preuniversitario → Universitario; `Sin nivel` queda al final.
- El banco de Lenguaje Algebraico se generó localmente desde el alcance de las
  transcripciones, sin Gemini ni llamadas externas.
- La herramienta aborta ante preguntas preexistentes y nunca reemplaza historial.
- Para la revisión v2 se cambió la política: el banco anterior se archivó para
  conservar cinco preguntas con respuestas históricas; no se eliminó historial.
- Los temas que requieren secuencia por número de clase deben usar
  `resource_ordering_method="manual"`; de otro modo el orden alfabético puede
  colocar sufijos como `4.01a` antes de `4.01`.
- Las prácticas se cuentan como preparadas con promedio reciente ≥80%; las
  evaluaciones conservan el aprobado histórico. Las estrellas siguen en backend,
  pero se ocultan en la página de tema.
- Los títulos públicos cambian sin regenerar slugs para no romper URLs ni SEO.

## Estado del proyecto / notas

- Producción responde HTTP 200.
- Lenguaje Algebraico: 17/17 recursos completos, 90 preguntas por recurso.
- Banco activo v2: 1.530 preguntas; banco anterior archivado: 1.535;
  cinco respuestas históricas preservadas.
- Taxonomía vigente:
  - Electromagnetismo → área Física.
  - Física Escolar → área Física y nivel Medio/Preuniversitario.
  - Matemática Media/Preuniversitaria → área Matemáticas.
- El recurso de Sonido de Física Escolar también quedó asociado a
  Medio/Preuniversitario, por lo que ya no aparece en `Sin nivel`.
- Evidencia detallada en
  `docs/auditorias/2026-06-21-lenguaje-algebraico-preguntas.md`.
- Rediseño de temas: 133 títulos limpiados localmente; segunda ejecución
  idempotente con 0 cambios. QA a 320/360/390 px sin overflow y reproductor
  fullscreen validado. Suite completa: **398 tests OK**.

## Pendientes / Próximos pasos

- Realizar QA humano por muestreo desde la interfaz de revisión si se desea
  ajustar tono, notación o dificultad de alguna clase específica.
- Rediseño compacto de temas integrado a `main` mediante el PR #77.
