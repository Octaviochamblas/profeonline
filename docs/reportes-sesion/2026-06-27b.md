# Reporte de sesión — 2026-06-27 (tarde)

## Avances desde el último reporte

### Estandarización de los 15 recursos ENTEROS_CONJUNTO

Se completó la estructura pedagógica completa para todos los recursos del bloque
`MAT.NUM.ENTEROS_CONJUNTO` (15 recursos). Cada YAML ahora tiene:

- 4 ejemplos: 2 abiertos + 2 interactivos Sí/No (con `respuesta:`)
- 5 `errores_frecuentes` (afirmaciones falsas que se usarán como preguntas conceptuales V/F)
- `estado: publicado`

**YAMLs actualizados e importados (todos 15):**
1. antecesor ✅  2. cardinales ✅  3. definicion ✅  4. impares ✅
5. orden-logica ✅  6. pares ✅  7. positivos-negativos ✅  8. recta-convencion ✅
9. recta-ubicacion ✅  10. sucesor ✅  11. tricotomia ✅ (esta sesión)
12. valor-absoluto-def ✅ (esta sesión)  13. valor-absoluto-notacion ✅ (esta sesión)
14. valor-absoluto-opuestos ✅ (esta sesión)  15. naturales ✅ (esta sesión)

Importados con `load_node_content`: 15/15 — Actualizados: 15, Errores: 0.

### Banco de ejercicios GEN: 150 ejercicios nuevos

Creados 3 archivos JSONL en `docs/conocimiento/ejercicios/`:
- `mat-num-enteros-conjunto-banco-gen-1.jsonl` — NAT, CARD, DEF, POS, RUB (50 ejercicios)
- `mat-num-enteros-conjunto-banco-gen-2.jsonl` — RCON, ORD, TRIC, VADEF, VANOT (50 ejercicios)
- `mat-num-enteros-conjunto-banco-gen-3.jsonl` — VAOPU, PAR, IMP, SUC, ANT (50 ejercicios)

Estructura por recurso: **3 conceptuales + 1 reconocimiento + 3 procedimiento_basico + 3 tipo_paes = 10 ejercicios**.
Stable_ids con prefijo `{ABBR}-GEN-{GRUPO}-{N}` para evitar colisión con ejercicios existentes.

Importados con `load_exercise_bank`: 150/150 — Creados: 150, Errores: 0.

### Fixes de sesión anterior (continuación)

- **`generate_node_summaries`**: fix encoding Windows (✓/✗ → OK/ERR), fix Gemini parsing
  multi-part (tomaba `parts[0]` vacío en vez de unir partes no-thought), sleep 1s→6s,
  backoff 429 → 30*(n+1)s, `maxOutputTokens` 300→800, `thinkingBudget: 0`.
- **`node_detail.html`**: resumen card reposicionado fuera del bloque `{% if practice_bank %}`,
  ahora visible para todos los recursos que tengan `content.resumen`.

## Decisiones importantes

- Stable_ids de los nuevos ejercicios usan prefijo corto (`NAT-GEN-`, `CARD-GEN-`, etc.)
  igual que el archivo `mat-num-enteros-conjunto-naturales.jsonl` original, en lugar del
  prefijo largo `CONJUNTO-*` del banco inicial. Es consistente.
- Los ejercicios tipo_paes tienen `"paes_style": true` y `competencia M1/M2`.
- Los procedimiento_basico son en su mayoría `true_false` para máxima claridad didáctica.

## Estado del proyecto / notas

- Los resúmenes IA (`resumen` field en `NodeContent`) siguen vacíos en 13 recursos;
  NATURALES (-206 chars) e IMPARES (-270 chars) tienen resúmenes generados en sesión anterior.
  Gemini tenía rate limit activo. Regenerar con `generate_node_summaries` cuando la cuota se
  restablezca (próximo día o después de varias horas).
- Los 15 recursos ahora tienen banco completo: ejercicios GEN + ejercicios anteriores.
  La sección "Practica" debería mostrar los 4 grupos.

## Pendientes / Próximos pasos

1. **Regenerar resúmenes IA** con `python manage.py generate_node_summaries` (cuota Gemini restablecida).
2. **Push a main** y desplegar para que los cambios de contenido sean visibles en producción.
3. **Verificar en navegador** que los 4 grupos de práctica aparecen correctamente en cada recurso.
4. **Continuar con otros bloques** del árbol ENTEROS (operatoria, etc.) usando el mismo pipeline YAML + JSONL.
