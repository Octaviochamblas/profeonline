# Reporte de sesión — 2026-06-22

## Avances desde el último reporte

- 🧩 Codex auditó el diff completo de `feat/guias-fase1-extraccion-items` contra `main`.
- Se revisaron el servicio IA, panel HTMX, persistencia, permisos, consultas, CSP, aislamiento por
  flag, fusión, pruebas y contraste con el handoff de la Fase 1.
- El gate fue rechazado por cinco P1 y cinco P2; los detalles y fixes sugeridos quedaron escritos en
  la tarjeta.
- La tarjeta se devolvió con `git mv` desde `backlog/4-auditoria/` a
  `backlog/3-construccion/`.

## Decisiones importantes

- No se modificó código funcional ni se hizo merge: la corrección corresponde a 🔨 Antigravity.
- Los P1 bloqueantes son: falta de enforcement del flag, fusión entre temas, JavaScript incompatible
  con CSP, descarte de `detected_exercise_count` y falta de validación guía↔tema.

## Estado del proyecto / notas

- `.venv\Scripts\python.exe manage.py test`: **425 tests OK** en 267.357 s.
- `check --deploy`: exit 0, siete warnings propios de settings locales.
- `makemigrations --check --dry-run`: `No changes detected`.
- `pre-commit run --all-files`: todos los hooks pasaron.
- `git diff --check`: sin errores.

## Cierre de Fase 1 por 🏛️ Claude (2026-06-22)

- 🔨 Antigravity **no subió correcciones** tras el rechazo (la rama seguía en `2a4efa6`). Por
  decisión del 🧑, 🏛️ Claude tomó excepcionalmente el rol de builder y corrigió **los 5 P1 + 5 P2**:
  - **P1:** flag por tema server-side en panel y mutaciones (`_get_enabled_topic`/`_get_enabled_item`);
    fusión solo dentro del mismo tema habilitado; JS movido a `static/js/item-extraction.js` con nonce
    (CSP) y sin `onclick`; campo `detected_exercise_count` (migración aditiva **0034**) persistido,
    mostrado y editable; validación de guía (`is_active` + relación con tema/asignatura).
  - **P2:** N+1 resuelto (`select_related`/`prefetch_related` + recursos del tema en memoria); dedupe
    de `suggested_resource_ids` y filtrado de IDs ajenos; validación de `level`/`difficulty` contra
    choices (400); `_sanitize_key` en el servicio y mensajes de error genéricos en la vista; +7 tests.
- **Barrera final (`.venv`):** `manage.py test` → **432 tests OK** (605 s) · `check --deploy` →
  exit 0 (7 warnings de settings locales) · `makemigrations --check --dry-run` → **No changes** ·
  `pre-commit run --all-files` → **Passed** · `git diff --check` → sin errores.
- **Squash-merge a `main`** (commit `6ccf403`). Tarjeta movida a `backlog/6-finalizados/`. Lock liberado.

## Pendientes / Próximos pasos

- **Fase 2** del epic: guía ProfeOnline original + validación de originalidad (anti-copia) +
  publicación manual. Núcleo legalmente sensible → `seguridad:requiere-claude`. Falta redactar el
  handoff de arquitectura y fijar el umbral anti-copia (decisión del 🧑).
- Desplegar: el push de `main` a Railway aplicará la migración `0034` (aditiva, sin riesgo).
