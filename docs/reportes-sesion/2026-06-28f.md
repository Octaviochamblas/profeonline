# Reporte de sesión — 2026-06-28f

## Avances desde el último reporte

- Se retomó la rama `content/mat-fund` para cerrar la construcción del eje `MAT.FUND`.
- Se completaron los 10 YAML faltantes y quedó el directorio con **106/106** archivos de contenido.
- Se corrigieron nombres de archivo del subtema `RAZONAMIENTO_LOGICO` para que coincidieran con
  los `semantic_id` reales.
- Se normalizaron y/o regeneraron los bancos `JSONL` de `MAT.FUND` hasta dejar **106 recursos con
  10 ejercicios cada uno**.
- Se corrigieron inconsistencias heredadas en bancos de ejercicios:
  - un `semantic_id` equivocado en `mat-fund-cardinalidad-banco-gen-1.jsonl`
  - dos `semantic_id` mal clasificados en el banco mixto de producto/cartesiano
  - dos archivos en esquema legacy (`enunciado`, `opciones`, `respuesta_correcta`) convertidos al
    esquema vigente del loader (`prompt`, `choices`, `correct_answer`)
- Se ejecutaron cargas locales exitosas:
  - `python manage.py load_node_content`
  - `python manage.py load_exercise_bank`
- Se verificó cobertura en DB:
  - `MAT.FUND.B0101: 43/43`
  - `MAT.FUND.B0102: 63/63`
  - `0` recursos con menos de 10 ejercicios
- Se ejecutó `python manage.py check` sin errores.
- Se ejecutó la suite completa `python manage.py test`: **587 tests OK**, `1` skipped.
- La tarjeta `poblar-contenido-mat-fund.md` quedó movida a `docs/backlog/4-auditoria/`.

## Decisiones importantes

- Se evitó seguir intentando generación externa con Gemini por fallas previas de escapes JSON y
  límites `429`; en su lugar se cerró el eje con generación local determinista.
- La primera corrida paralela de validaciones produjo un `database is locked` de SQLite para
  `load_exercise_bank`; se reejecutó en serie y no dejó efectos persistentes.
- Se mantuvo fuera del commit cualquier script auxiliar en `scratch/` o archivos de apoyo no
  pertenecientes al entregable final.

## Estado del proyecto / notas

- Rama activa: `content/mat-fund`.
- Construcción `MAT.FUND` terminada y lista para auditoría técnica formal.
- `docs/_coordinacion/ESTADO.md` debe liberarse al cerrar el commit de esta sesión.

## Pendientes / Próximos pasos

- Staging selectivo de `docs/conocimiento/contenido/mat-fund-*.yaml`,
  `docs/conocimiento/ejercicios/mat-fund-*.jsonl`, la tarjeta movida y este reporte.
- Commit de cierre de construcción en `content/mat-fund`.
- Auditoría técnica de la etapa `4-auditoria` sobre el diff final antes de pasar a cierre.
