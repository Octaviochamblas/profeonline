# Reporte de sesión - 2026-06-02

## Avances desde el último reporte

- Se auditó en profundidad la implementación C1 de Antigravity en la rama `fix/seed-idempotente`.
- Se revisó el diff contra `main`, incluyendo:
  - `apps/content/management/commands/seed_math_resources.py`
  - `apps/content/tests/test_management_commands.py`
  - `Procfile`
  - `nixpacks.toml`
  - `docs/backlog/4-auditoria/c1-seed-idempotente.md`
  - `.claude/settings.local.json`
  - `build.sh`
- Se creó la auditoría formal `docs/auditorias/2026-06-02-seed-idempotente-c1.md`.
- Se actualizó el índice canónico de auditorías en `docs/auditorias/README.md`.

## Decisiones importantes

- La implementación funcional de C1 se considera técnicamente correcta: el seed ya no pisa recursos
  existentes en una corrida normal y salió del arranque principal de Railway.
- No conviene aprobar aún con `audit:aprobado` hasta retirar o justificar el cambio fuera de
  alcance en `.claude/settings.local.json`.
- C1 no debería marcarse como mitigado completamente hasta alinear `build.sh` y la documentación
  operacional viva.

## Verificación

- `.venv\Scripts\python.exe manage.py test apps.content.tests.test_management_commands.SeedMathResourcesCommandTests --verbosity 1`: OK, 2 tests.
- `.venv\Scripts\python.exe manage.py check`: sin issues.
- `.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`: sin cambios detectados.

## Pendientes / Próximos pasos

1. Sacar `.claude/settings.local.json` del PR o revisarlo como PR separado de gobernanza.
2. Retirar `seed_math_resources` de `build.sh` o documentar formalmente que no es parte del deploy.
3. Actualizar `docs/README.md`, `docs/gobernanza/inventario-operacional.md`,
   `docs/gobernanza/matriz-riesgos.md` y `docs/gobernanza/roadmap-priorizado.md`.
4. Considerar test adicional para preservar `levels` editados manualmente.
5. Crear tarjetas futuras C1b/C1c/C1d para clave estable de seed, runbook manual y guardrail CI.

---

# Auditoría C1b - seed_content idempotente

## Avances desde el último reporte

- Se auditó la rama `fix/seed-content-idempotente` dejada por Antigravity.
- Se revisó el diff contra `main`, incluyendo:
  - `apps/content/management/commands/seed_content.py`
  - `apps/content/management/commands/seed_resources.json`
  - `apps/content/tests/test_management_commands.py`
  - `docs/backlog/4-auditoria/c1b-seed-content-idempotente.md`
- Se creó la auditoría formal `docs/auditorias/2026-06-02-seed-content-idempotente-c1b.md`.
- Se actualizó el índice de auditorías.

## Decisiones importantes

- El código C1b es aprobable: se reemplaza `update_or_create` por `get_or_create`, `levels.set(...)`
  queda limitado a creación, y `--refrescar-seo` solo toca `description`/`content`.
- No se encontraron hallazgos bloqueantes.
- Se anotaron riesgos preventivos no bloqueantes: referencia de módulo a un título ausente en el
  JSON, cobertura no-clobber no explícita para todos los modelos y deuda de clave estable por slug.

## Verificación

- `.venv\Scripts\python.exe manage.py test apps.content.tests.test_management_commands.SeedContentCommandTests --verbosity 1`: OK, 4 tests.
- `.venv\Scripts\python.exe manage.py test apps.content.tests.test_management_commands --verbosity 1`: OK, 7 tests.
- `.venv\Scripts\python.exe manage.py check`: sin issues.
- `.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`: sin cambios detectados.
- `git diff --check main...HEAD`: sin problemas.

## Pendientes / Próximos pasos

1. Confirmar CI verde del PR #27.
2. Si el PR remoto coincide con esta rama, agregar `audit:aprobado`.
3. Dejar los riesgos preventivos como hardening posterior si se quiere endurecer seeds y módulos.

---

# Construcción C2 - Backups verificados + drill de restauración

## Avances desde el último reporte

- Se implementaron los comandos personalizados de Django:
  - `backup_db.py`: Respalda SQLite (vía API nativa de respaldo) y PostgreSQL (vía wrapper seguro sobre `pg_dump` usando la variable `PGPASSWORD` para no exponer secretos en los procesos).
  - `restore_db.py`: Restaura SQLite y PostgreSQL (`pg_restore` con flags `-O -x -c` para limpieza y permisos). Incorpora **guardas de seguridad obligatorias** (rechaza la ejecución en entornos de producción con `DEBUG=False` o base de datos con host remoto a menos que se provean `--confirmar` y `--destino [db_name]`).
- Se redactó el runbook canónico `docs/gobernanza/runbook-backups.md`, el cual describe los procedimientos y deja la evidencia completa del drill de recuperación local (conteo de filas de entidades taxonómicas y pedagógicas verificado contra base de datos origen).
- Se agregaron las pruebas unitarias correspondientes en `apps/core/tests.py`, probando los casos de éxito local y los bloqueos y mocks de entorno remoto/producción.

## Decisiones importantes

- La API de SQLite `Connection.backup` se usó en lugar de copiar archivos para asegurar la compatibilidad con bases de datos SQLite en memoria que Django crea para las suites de prueba.
- Para PostgreSQL, se captura y limpia la salida de error en caso de fallo, reemplazando cualquier mención de la contraseña de la DB por `********` para evitar fugas accidentales en los logs.
- Se movió la tarjeta `c2-backups-restore-drill.md` de la etapa `3-construccion` a `4-auditoria`.

## Verificación

- `.venv\Scripts\python.exe manage.py test apps.core.tests.BackupRestoreCommandTests`: OK, 5 tests.
- Barrera local completa: `.venv\Scripts\python.exe manage.py test` -> OK, 178 tests.
- Django system check: Exitoso (1 advertencia esperada sobre LocMemCache en producción).
- Django missing migrations check: Exitoso, sin cambios de modelo pendientes.

## Pendientes / Próximos pasos

1. Confirmar CI verde de PR #28.
2. Permitir que Codex audite el PR #28 y ponga el label `audit:aprobado` para su posterior squash-merge.

---

# Auditoría y cura C2 - Backups verificados + drill

## Avances desde el último reporte

- Se auditó la rama `feat/backups-restore-drill` dejada por Antigravity.
- Se revisaron los comandos `backup_db` y `restore_db`, el runbook, la tarjeta C2 y los tests.
- Se creó la auditoría formal `docs/auditorias/2026-06-02-backups-restore-drill-c2.md`.
- Se curaron tres puntos preventivos:
  - `backups/` agregado a `.gitignore` para evitar commits accidentales de dumps.
  - `restore_db` ahora exige `--permitir-remoto` además de `--confirmar` y `--destino` para destinos PostgreSQL remotos o `DEBUG=False`.
  - El runbook ya no afirma que Railway tiene backups automáticos activos; queda pendiente porque el plan actual no los ofrece.

## Decisiones importantes

- C2 queda aprobable como herramienta manual y drill local, pero no como cierre total del riesgo de continuidad operacional.
- Backups automáticos del proveedor quedan diferidos hasta tener contenido real crítico o contratar/configurar una alternativa.

## Verificación

- `.venv\Scripts\python.exe manage.py test apps.core.tests.BackupRestoreCommandTests --verbosity 1`: OK, 6 tests.
- `.venv\Scripts\python.exe manage.py check`: sin issues.
- `.venv\Scripts\python.exe manage.py makemigrations --check --dry-run`: sin cambios detectados.
- `git diff --check`: sin errores.

## Pendientes / Próximos pasos

1. Confirmar CI verde de PR #28 si el PR remoto incluye esta cura.
2. Describir PR #28 como C2a/manual tooling + drill local, no como backups automáticos cerrados.
3. Cuando exista contenido real o plan compatible, activar backups del proveedor/externos y ejecutar restore desde backup real.
